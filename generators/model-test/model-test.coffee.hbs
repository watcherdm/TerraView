{{#camelize}}{{name}}{{/camelize}}Model = require "models/{{#camelize}}{{name}}{{/camelize}}"

describe "Model {{#camelize}}{{name}}{{/camelize}}", ->
  beforeEach ->
    @model = {{#camelize}}{{name}}{{/camelize}}Model.create()

  afterEach ->
    @model.dispose()

    expect({{#camelize}}{{name}}{{/camelize}}Model.getPool().isUsed.length()).to.equal 0
